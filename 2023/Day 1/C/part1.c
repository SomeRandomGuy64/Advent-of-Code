#include <stdio.h>

int main(void) {
    FILE* file_ptr;

    file_ptr = fopen("../input.txt", "r");

    if (file_ptr == NULL) {
        printf("file can;t be opened \n");
    }

    while (fgets) {
		printf("testing github");
    }

    return 0;
}
